#include <stdio.h>
#include <math.h>
int main()
{
    double t, g, tk, n;
    scanf("%lf %lf", &t, &g);
    while(g>0)
    {
        n= sqrt(t);
        
        tk= pow(n/2,2);
        
        tk= pow(2,2);

    printf("%lf", tk);
      g--;
    }
}